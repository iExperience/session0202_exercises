<h1>Places</h1>

<table>
	<% @places.each do |place| %>
	<tr>
		<td><%= place.name %> </td>
		<td><%= link_to 'Show', place_path(place) %></td>
	</tr>
	<% end %>
</table>

</br>
<%= link_to 'New place', new_place_path %>